# Core demo dependencies — kept minimal so Streamlit Community Cloud can install.
# Lower bounds only, so the cloud's Python 3.13 picks compatible wheels.
streamlit>=1.40
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.9
python-dotenv>=1.0
requests>=2.32
httpx>=0.27
pandas>=2.2
numpy>=1.26
plotly>=5.24

# Optional LLM providers — install ONLY if you set LLM_PROVIDER=watsonx or =custom.
# Both are imported lazily so the mock demo runs without them.
#
#   pip install ibm-watsonx-ai>=1.1
#   pip install openai>=1.50
